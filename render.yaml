services:
  - type: web
    name: portfolio
    runtime: static
    buildCommand: echo "No build step required"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html